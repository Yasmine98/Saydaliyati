<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorOnSurface">@color/textOnSurfaceColor</item>
        <item name="colorSurface">@color/surfaceColor</item>

        <item name="android:windowBackground">@color/bcColor</item>
        <item name="actionBarStyle">@style/AppTheme.AppBarOverlay</item>
    </style>
    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- android:textColorPrimary is the color of the title text in the Toolbar  -->
        <item name="android:textColorPrimary">@color/primaryTextColor</item>
        <!-- actionMenuTextColor is the color of the text of action (menu) items  -->
        <item name="actionMenuTextColor">@color/primaryTextColor</item>
        <!-- Tints the input fields like checkboxes and text fields -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Applies to views in their normal state. -->
        <item name="colorControlNormal">@color/primaryTextColor</item>

        <!-- Enable these below if you want clicking icons to trigger a ripple effect -->
        <!--
        <item name="selectableItemBackground">?android:selectableItemBackground</item>
        <item name="selectableItemBackgroundBorderless">?android:selectableItemBackground</item>
        -->
    </style>
    <style name="AppTheme.MyCardView" parent="CardView.Light">
        <!-- Customize your theme here. -->
        <item name="cardElevation">4dp</item>
        <item name="cardCornerRadius">4dp</item>
        <item name="android:layout_marginTop">8dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="android:text">@color/primaryTextColor</item>
    </style>
    <style name="AppTheme.AppBarOverlay.Started" parent="AppTheme.AppBarOverlay">
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="android:text">@color/primaryTextColor</item>
        <item name="android:windowBackground">@color/bcColor</item>
    </style>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="android:text">@color/primaryTextColor</item>
    </style>
    <style name="MaterialSwitch">
        <item name="sw_trackSize">30dp</item>
        <item name="sw_trackCap">round</item>
        <item name="sw_thumbRadius">16dp</item>
        <item name="sw_thumbElevation">2dp</item>
        <item name="sw_animDuration">@android:integer/config_shortAnimTime</item>
        <item name="sw_interpolator">@android:anim/decelerate_interpolator</item>
    </style>

</resources>
